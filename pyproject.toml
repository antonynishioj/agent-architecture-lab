[project]
name = "agent-architecture-lab"
version = "0.1.0"
description = "Experimental framework for measuring the quality, cost, latency, and reliability of different AI agent architectures."
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "google-genai==2.24.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "requests",
    "uvicorn[standard]",
]